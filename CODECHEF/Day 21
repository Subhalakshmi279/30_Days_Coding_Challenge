Attack the kingdom
#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--){
	    int n;
	    cin>>n;
	    int temp[n];
	    for(int i=0;i<n;i++){
	        cin>>temp[i];
	    }
	    sort(temp,temp+n);
	    cout<<temp[1]<<endl;
	}
	return 0;
}
