Chef and Chocolates:
#include <iostream>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int x, y, z;
        cin >> x >> y >> z;
        if (y <= (x * 5 + y * 10))
        {
            cout << (x * 5 + y * 10) / z << endl;
        }
    }
    return 0;
}
